export default interface Permissions {
  global: boolean;

  // public hasPermission(permission: PermissionType): boolean {
  //   return this.permissions.indexOf(permission) >= 0;
  // }
  //
  // public hasGlobalPermission(): boolean {
  //   return this.hasPermission(PermissionType.global);
  // }
}
